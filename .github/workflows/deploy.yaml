name: Deploy

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v4

    - name: install zig
      uses: mlugg/setup-zig@v1
      with:
        version: 0.14.0

    - name: build engine
      run: |
        cd engine
        zig build --release=fast
